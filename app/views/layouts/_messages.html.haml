.notification-area
  - flash.each do |name, msg|
    - if msg.is_a?(String)
      %div{:class => "alert #{params[:controller]} alert-#{name}"}
        %a.close{"data-dismiss" => "alert"} ×
        = content_tag :div, msg, :id => "flash_#{name}"
