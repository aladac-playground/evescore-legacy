.well
  %h4
    Top
    = params[:action].capitalize
  %i
    Ranking of kills of NPCs classified as "Deadspace Overseer", usually found in DED Complexes and Level 4 and 5 missions
    
  %table.table.table-striped
    %thead
      %tr
        %th Rank
        %th Character Name
        %th.numeric Rats Killed
    - i = 0
    - @income.each do |char|
      - i+=1
      - next if char.name.nil?
      %tr
        %td= i
        %td
          = eve_image :char, char.id, 32
          = link_to char.name, char_path(char.id)
        %td.numeric
          %i= nice_number char.amount 
          kills
